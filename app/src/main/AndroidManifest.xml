<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.jaus.albertogiunta.justintrain_oraripendolaritrenitalia"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan|adjustResize">
        <activity android:name=".journeySearch.JourneySearchActivity">
        </activity>
        <activity android:name=".journeyResults.JourneyResultsActivity">
        </activity>
        <activity
            android:name=".favourites.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".journeySearch.StationSearchActivity"
            android:launchMode="singleTop">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.SEARCH" /> -->
            <!-- </intent-filter> -->
            <!-- <meta-data android:name="android.app.default_searchable" -->
            <!-- android:value=".journeys.StationSearchActivity" /> -->
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <service
            android:name=".notification.NotificationService"
            android:exported="false">
        </service>

        <activity android:name=".journeyResults.JourneyResultsActivity">
        </activity>
    </application>

</manifest>